To generate the embeddings, the code is 
```` 
python driver.py 
````
Before that, 
1. Install **pytorch_tabnet** 
2. Run **generateEmbeddings.py** by 
    a. Select the categorial values in the source data(app and machine name in given code)
    b. Make necessary changes to make it match the dimension of the data in the code